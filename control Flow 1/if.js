// ============================ IF ==============================
//Syntax : if(condition){ }       Condition must be true
const user={
    username:"Rabindra",
    Loggedin:true,
    age:23
}
if (user.Loggedin)
{
    console.log(`${user.username} is LoggedIN and age is ${user.age}`);
    
}